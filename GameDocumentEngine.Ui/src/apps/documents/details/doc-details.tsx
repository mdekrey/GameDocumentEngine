export function DocumentDetails({
	gameId,
	documentId,
}: {
	gameId: string;
	documentId: string;
}) {
	// TODO: load document
	// TODO: load game type objects
	// TODO: display component of game type with loaded document

	return (
		<>
			Doc Details {gameId} {documentId}
		</>
	);
}
